#include "main.h"
/**
 *_abs - computes the abs value
 *@r: num to be computed
 *
 *Return: rturns an integer
 */
int _abs(int r)
{
	if (r < 0)
	{
		r = r * -1;
		return (r);
	}
	else
	{
		return (r);
	}
}
